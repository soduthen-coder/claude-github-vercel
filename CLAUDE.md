# 클로드 → 깃허브 → 버셀 · 실행 지침

이 파일은 **Claude Code 가 자동으로 읽습니다.** 사용자가 설명하지 않아도
아래대로 움직이세요.

## 이 도구의 전제

사용자가 클로드에게 무언가 만들어 달라고 해서 **결과물이 생겼습니다.**
그것을 깃허브에 올리고 인터넷 주소로 열리게 하는 것 — 그게 전부입니다.

빈 사이트를 새로 만드는 도구가 아닙니다. **이미 있는 것을 올리는 도구**입니다.

**대상은 완전 초보입니다.** 클로드 계정을 막 만들었고, 깃허브도 버셀도
터미널도 모릅니다. 그러니 **사용자에게 명령을 시키지 마세요. Claude 가
직접 실행합니다.**

---

## 언제 쓰는가

무언가를 만들어 준 뒤, 사용자가 이렇게 말하면 **되묻지 말고 바로 실행**합니다.

| 사용자의 말 | 실행 |
|---|---|
| "올려 줘", "배포해 줘", "인터넷에 올려 줘" | `bash <도구>/ship.sh --dir <결과물폴더>` |
| "깃허브에 올려 줘" | 같음 (깃허브와 버셀은 한 번에 갑니다) |
| "고친 거 다시 올려 줘" | `bash <도구>/ship.sh --dir <폴더> "무엇을 고쳤는지"` |
| "남들도 보게 해 줘", "링크 공유할 거야" | `bash <도구>/ship.sh --dir <폴더> --public` |
| "나만 보게 해 줘", "잠가 줘" | `bash <도구>/lock.sh <이름> on` |
| "다시 공개해 줘" | `bash <도구>/lock.sh <이름> off` |
| "내려 줘", "지워 줘" | 아래 **지우기** 항목을 따르세요 |

사용자가 이 저장소 주소만 붙여넣었다면, 내려받고 바로 쓸 준비를 합니다.

```bash
git clone https://github.com/soduthen-coder/claude-github-vercel.git
```

---

## 만들어 준 직후에는 먼저 물어보세요

무언가를 만들어 줬는데 사용자가 아직 배포 얘기를 안 했다면, **한 줄로**
물어보세요. 길게 설명하지 마세요.

> 인터넷 주소로 올려 드릴까요? 남들도 링크로 열 수 있게 됩니다.

"응" 하면 바로 `ship.sh` 를 실행합니다. 그 이상 묻지 마세요.

---

## 사람이 직접 할 일은 두 번뿐입니다

`ship.sh` 가 준비되지 않은 것을 알아서 갖춥니다. 사람의 손이 필요한 곳은
**최초 로그인 두 번**뿐이고, 그것도 계정당 한 번입니다.

| 단계 | 누가 |
|---|---|
| git · Node.js 설치 | Claude (winget / brew) |
| gh · vercel 설치 | Claude |
| **깃허브 로그인** | **사용자** — 8자리 코드를 브라우저에 입력 |
| **버셀 로그인** | **사용자** — Continue with GitHub 클릭 |
| 저장소 만들기 · 올리기 · 배포 · 확인 | Claude |

로그인이 필요해지면 화면에 나온 **8자리 코드를 크게 보여 주고**, 어디에
넣는지 알려 주세요. 코드·비밀번호·토큰을 **대신 입력하지 마세요.**

계정이 없다면 가입 주소를 안내하세요.

- 깃허브 https://github.com/signup
- 버셀 https://vercel.com/signup — **Continue with GitHub** 을 고르면 빠릅니다

**2단계 인증(Authenticator)은 권하지 마세요.** 가입에도 배포에도 필요 없습니다.

---

## 지우기

되돌릴 수 없으므로 **두 단계로** 합니다.

```bash
bash <도구>/down.sh <이름>          # 1) 무엇이 지워지는지 보여만 줍니다
bash <도구>/down.sh <이름> --yes    # 2) 사용자가 그렇게 하겠다고 하면 실행
```

1번 결과를 사용자에게 그대로 보여 주고 확인을 받으세요. **대화에서 받은
그 답이 확인 절차입니다.** 확인 없이 `--yes` 를 붙이지 마세요.

---

## 반드시 지킬 것

### 결과는 추정하지 말고 확인해서 보고

스크립트가 이미 로그인하지 않은 상태로 실제 접속해 결과를 출력합니다.
그 출력을 근거로 보고하세요. "올렸습니다" 라고만 하지 않습니다.

**잠금 여부는 페이지 제목으로 판별합니다.** 버셀은 잠겨 있어도 HTTP 200 을
반환하므로 코드만 보면 틀립니다.

- `Login – Vercel` → 나만 보기
- 그 외 제목 → 공개

### 저장소 공개 ≠ 사이트 공개

| | 기본값 |
|---|---|
| 깃허브 저장소 (코드) | 비공개 |
| 버셀 사이트 (웹페이지) | **공개** |

저장소를 비공개로 뒀다고 사이트까지 가려지는 것이 아닙니다.
**개인정보나 민감한 데이터가 담긴 결과물이면 `--lock` 을 함께 주고 그 사실을
사용자에게 알리세요.**

반대로 "과제 제출", "링크 공유" 처럼 남이 열어야 하는 맥락이면 잠그면 안 됩니다.

### 이름은 알아서 처리됩니다

한글 이름을 그대로 넘겨도 됩니다. `lib/slug.js` 가 로마자로 옮기고
무엇으로 바뀌었는지 알려 줍니다. 화면에 보이는 제목은 건드리지 않습니다.

- 포트폴리오 → `poteupolrio`
- 대전 서구 상권 → `daejeon-seogu-sanggwon`

### 커넥터·플러그인을 쓰지 마세요

이 도구는 `git` · `gh` · `vercel` 세 가지 명령줄 프로그램만 씁니다.
커넥터가 연동되어 있어도 **쓰지 마세요.** 충돌하지는 않지만, 커넥터는
권한 범위가 계정마다 다르고 사용자가 모르는 사이 달라집니다.

실제로 겪은 것:

- 커넥터로 저장소 생성 → `403 Resource not accessible by integration`
- 커넥터로 버셀 프로젝트 조회 → `404` (만들어졌는데도 안 보임)
- **CLI 로 바꾸니 전부 정상 동작**

**도구가 404 나 403 을 내면 계정 상태를 의심하기 전에 도구의 권한 범위를
먼저 확인하세요.** 한 번은 이걸 거꾸로 짚어, 사용자를 필요도 없는 2단계 인증
설정으로 보낸 일이 있었습니다.

---

## 알려진 함정 (겪고 고친 것들)

| 증상 | 원인 | 해결 |
|---|---|---|
| 저장소 이름이 `-` 로 생성됨 | 한글 이름 | `lib/slug.js` 로 자동 변환 |
| `Detected linked project does not have "id"` | 한글 폴더명 | 영문 폴더명 |
| 버셀 목록에서 아무것도 못 찾음 | **vercel 이 표를 stderr 로 출력** | `2>&1` 로 합쳐 읽기 |
| 버셀·node 가 `C:\tmp\...` 를 못 찾음 | Git Bash 가 경로를 윈도우식으로 변환 | `winpath()` (`cygpath -m`) |
| API 경로가 `C:/Program Files/Git/v9/...` 로 바뀜 | 같은 원인 | `MSYS_NO_PATHCONV=1` |
| 스크립트가 조용히 죽음 | `grep` 실패를 `pipefail` 이 오류로 간주 | 파이프라인 끝에 `\|\| true` |
| 잠근 줄 알았는데 열림 | 기본값이 `all_except_custom_domains` | `deploymentType: "all"` |
| 지운 직후 확인이 "살아 있음" | 버셀 전파가 확인보다 느림 | 404 나올 때까지 재시도 |
| 남의 사이트 상태를 내 것처럼 보고 | 주소를 `<이름>.vercel.app` 으로 추측 | 추측하지 않고 버셀이 준 주소만 사용 |
| `.cmd` 파일이 한글 줄을 명령으로 실행 | cmd.exe 가 UTF-8 을 못 읽음 | `.cmd` 는 ASCII 로만 작성 |

## 버셀 깃 연동을 쓰지 않는 이유

깃허브 저장소를 버셀에 연결하면 `push` 만으로 자동 배포됩니다. 하지만
그러려면 **버셀 깃허브 앱에 저장소 접근 권한을 사람이 직접 켜야** 합니다
(`github.com/settings/installations`). 새 저장소를 만들 때마다 거기서 막힙니다.

그래서 이 도구는 연동 대신 **스크립트가 직접 배포**합니다. 결과는 같고
사람 손이 들지 않습니다.
